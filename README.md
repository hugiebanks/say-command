# Say Command

Simple `/say` command for Discord.

### What it does
- Only admins can use it
- Bot sends whatever you type
- 10 second cooldown
- Deletes the "done" message after 3 seconds

### Setup
1. Put `say.js` in your commands folder
2. Make sure the bot has **Send Messages** permission
3. Restart the bot

That’s it.